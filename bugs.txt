On the student side, whenever the student section for any of these details in the profit section, it doesn't appear whenever I click the changes just refresh and all the gone is not saved even on the refresh it just refresh and makes it blank. It should not be like that it needs to solve the problem. It should save the detail instead of everything removing it while refresh.

Another thing is in the sidebar on the admin and the student, both side like the side bar is movable, but for example whenever I remove the sidebar and adjust width of the side bar and then click on the any of the button. For example, if I am currently on the profile, I click on the document. The sidebar automatically goes to the default. It should not be like that, otherwise, every time user has to access sidebar, so instead of fit, whatever user set it at, it should stay there instead of moving get back to the default.

And the admin side in the applicant profile section. There is an unknown applicant visible who is only on the registered state. The car and the list is visible in this section. But when I click the applicant's card, it just shows me that applicant doesn't exist or no applicant confirm this. Sir needs to be solved. I don't know why is this not visible?